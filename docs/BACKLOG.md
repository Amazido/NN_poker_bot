# Бэклог

Что собираемся делать и какие решения по дороге уже приняты. Документ живой —
правится по мере движения. Что **уже сделано** и почему — в
[CHANGELOG.md](./CHANGELOG.md). Как система устроена **сейчас** —
в [ARCHITECTURE.md](./ARCHITECTURE.md).

Правило переноса: как только пункт выпущен, его описание переезжает в
`ARCHITECTURE.md` (актуальное состояние) и получает запись в `CHANGELOG.md`
(что изменилось и почему), а отсюда удаляется.

---

## Дорожка

### ~~Этап 1 — каркас конфигурации правил~~ — выпущен 2026-09-24

См. `ARCHITECTURE.md` (раздел «Конфигурация правил») и запись в `CHANGELOG.md`.

### ~~Этап 2 — колода и заказ вслепую~~ — выпущен 2026-09-24

См. `ARCHITECTURE.md` (разделы «Безлимитная колода» и «Заказ вслепую») и запись
в `CHANGELOG.md`.

### ~~Конструктор правил стола~~ — выпущен 2026-09-24

См. `ARCHITECTURE.md` (раздел «Настройки стола») и запись в `CHANGELOG.md`.

### Этап 3 — сезоны, рейтинг, награды

Отдельный большой заход. Понятие сезона, общий лидерборд, система рейтинга,
награды. Требует отдельного разговора по визуалу — экранов здесь больше, чем во
всей остальной игре.

Подготовка сделана: эталонная редакция сезона 1 существует как
`odessa_season_1 v1` во встроенном каталоге, играбельна и покрыта тестами, а у
стола появился признак «играет по редакции, а не по своим правилам» — на нём и
строится отбор сезонных матчей. Осталось само понятие сезона.

Что предстоит решить и сделать:

- где живёт привязка «сезон → редакция» (см. «Открытые вопросы»);
- хранение результатов матчей за пределами одной комнаты;
- формула рейтинга и лидерборд;
- экраны: лидерборд, профиль, награды, история матчей.

Отдельный вопрос: считать ли в зачёт сезона столы, собранные вручную, если их
правила случайно совпали с эталонными. Сейчас признак опирается на способ
создания (`rules_edition_id` против `rules_config`), а не на содержимое конфига.

---

## Отложено

### Одновременные заказы (`bidding.simultaneous`)

Все заказывают в одно общее окно, не видя чужих заказов. Ортогонально заказу
вслепую: включаются независимо и сочетаются (тогда игрок не видит ни своей
руки, ни чужих ставок).

Отложено, потому что сезон 1 играется последовательным заказом.

Почему это дорого: сейчас торги — строгая очередь (`bid_turn` идёт по кругу).
Одновременный режим меняет машину состояний раздачи, публичный вид (заказы
нельзя показывать до вскрытия), таймер хода (общий дедлайн вместо
персонального) и экран торгов на фронте.

**Решение принято заранее:** при `simultaneous` крюк отключается. Последнего
заказывающего не существует, цеплять правило не к чему.

---

## Пространство правил (цель)

Таблицы описывают **целевой** конфиг, а не текущий. Реализованное по мере
выпуска переезжает в `ARCHITECTURE.md`.

### Переключатели

| Опция | Что делает | Состояние |
|---|---|---|
| `ranking.two_beats_ace_same_suit` | двойка бьёт туза своей масти, если оба в одном сбросе | **готово** |
| `ranking.offcolor_beats_oncolor` | некозырной джокер бьёт козырного | **готово** |
| `bidding.hook` | сдающий не может сделать сумму заказов равной числу карт | **готово** |
| `ranking.duplicate_first_wins` | при одинаковых картах взятку берёт положивший раньше | **готово** |
| `deck.infinite` | бесконечная колода, карты повторяются | **готово** |
| `bidding.blind_allowed` | можно заказать, не открывая руку | **готово** |
| `bidding.simultaneous` | все заказывают в одно окно, не видя чужих | отложено |

### Числовые параметры

Все перечисленные ниже — **готовы** и настраиваются редакцией.

| Параметр | Классика | Сезон 1 |
|---|---|---|
| `rounds.start` / `rounds.peak` | 1 / 10 | 1 / 10 |
| `deck.jokers` | 2 | 2 |
| `players.min` / `players.max` | 3 / 5 | 3 / 5 |
| `scoring.exact_per_trick` | 10 | 10 |
| `scoring.exact_zero_bonus` | 10 | 10 |
| `scoring.over` | −5 | −5 |
| `scoring.under_per_trick` | −10 | **−5** |
| `scoring.blind_bonus` | 0 | 5 |
| `turn_timeout_sec` | 30 | 30 |

### Связанные и несовместимые сочетания

Семь переключателей дают 128 комбинаций на бумаге, но часть из них лишена
смысла. Осмысленных — 72.

- `ranking.duplicate_first_wins` работает только при `deck.infinite`. В обычной
  колоде двух одинаковых карт не бывает, флаг ни на что не влияет.
- `bidding.hook` несовместим с `bidding.simultaneous` — отключается
  принудительно (см. «Отложено»).

Гонять все 72 сочетания смысла нет. Проверяем каждый переключатель отдельно
против базовой редакции плюс пересечения, где правила трутся друг о друга.
Все перечисленные ниже пересечения закрыты тестами (`tests/test_cards.py`,
`tests/test_blind_bidding.py`, `tests/test_autoplay.py`):

- **`deck.infinite` + два одинаковых джокера в одной взятке.** Было самым
  опасным местом: «первый выигрывает» получалось случайно из `max()`/`next()`.
  Теперь это явный флаг `duplicate_first_wins` с тестами на обе стороны.
- **`deck.infinite` + `two_beats_ace_same_suit`.** `_effective_rank` ищет туза
  той же масти среди сыгранных карт. При повторах в одной взятке может
  оказаться несколько двоек и несколько тузов одной масти.
- **`deck.infinite` + одинаковые козыри в одной взятке.**
- **`blind_allowed` + `hook` у сдающего.** Сдающий обязан не добить сумму
  заказов до числа карт, не видя своей руки. Правило работает, играть тяжело —
  похоже, ровно ради этого вслепую и вводится.

### Эталонная редакция сезона 1

Заведена как `odessa_season_1 v1` в `backend/app/poker/editions.py` — играбельна
и покрыта тестами. Отличия от классики: двойка бьёт туза, некозырной джокер
бьёт козырного, недобор подешевел с −10 до −5 за взятку, появился заказ вслепую
(+5 за точный заказ).

Чем сезон её «назначит» эталонной — вопрос этапа 3. Сама редакция уже
неизменяема: правки выпускаются версией `v2`, а не переписыванием `v1`.

---

## Принятые решения

Чтобы не переобсуждать. Решения по уже выпущенному переезжают в `CHANGELOG.md`.

**Крюк при одновременных заказах отключается.** Цеплять правило не к чему.

**Заказ вслепую и одновременные заказы — разные механизмы.** Первый про свою
руку, второй про чужие ставки. Настраиваются независимо, сочетаются.

**Конфиг редакции снапшотится в состояние матча при старте** (`state["rules"]`).
Правка редакции не меняет правила идущих матчей. Развязка, без которой сезоны
невозможны; закреплена тестом `test_running_match_keeps_its_rules_snapshot`.

**Изменение правил выпускается новой версией редакции**, существующая не
правится: иначе поменяется смысл уже сыгранных матчей, которые на неё ссылаются.

**Условие приёмки любого нового правила:** авто-ход (`poker/autoplay.py`) умеет
всё, что умеет живой игрок, — иначе бот или истёкший таймер подвесит раздачу.
Проверяется прогоном полного матча ботами на новой редакции
(`tests/test_autoplay.py`).

**Новое правило попадает в форму создания стола, если форма его выражает.**
Иначе редакция остаётся в каталоге, но заготовкой не показывается: подставленные
поля соврали бы о том, что написано на кнопке.

---

## Открытые вопросы

- **Где живёт эталонная редакция сезона** — обычная запись в `rules_editions`
  с пометкой в `meta`, или отдельная таблица сезонов. Решается на этапе 3.
